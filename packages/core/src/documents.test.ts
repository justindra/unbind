// import { generateFileS3Key, getMetadataFromS3Key } from './documents';

// describe('documents', () => {
//   describe('S3 Key Generation', () => {
//     it('should generate a key and be able to parse it back', () => {
//       const org = 'org';
//       const doc = 'doc';
//       const file = 'file';
//       const key = generateFileS3Key(org, doc, file);
//       const metadata = getMetadataFromS3Key(key);
//       expect(metadata.organizationId).toEqual(org);
//       expect(metadata.documentId).toEqual(doc);
//       expect(metadata.fileId).toEqual(file);
//     });
//   });
// });
